[tool.poetry]
name = "foodmenu"
version = "0.1.0"
description = ""
authors = ["Noë Snaterse <noe@wiredpea.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^5.0.4"
ruff = "^0.4.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
